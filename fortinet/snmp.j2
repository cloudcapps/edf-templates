config system snmp user
    edit "{{snmp.read_only.user}}"
        set trap-status disable
        set source-ip "{{device.primary_ip.address.ip}}"
        set security-level auth-priv
        set auth-pwd {{snmp.read_only.auth}}
        set priv-pwd {{snmp.read_only.priv}}
    next
end