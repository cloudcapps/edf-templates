config log syslogd setting
    set status enable
    set server "{{syslog.servers[0]}}"
    set port {{syslog.port}}
    set source-ip "{{device.primary_ip.address.ip}}"
    set interface-select-method sdwan
end